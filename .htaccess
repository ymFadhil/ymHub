# -----------------------------
# 0️⃣ Enable symlinks (LiteSpeed sometimes needs this)
Options +FollowSymLinks

# -----------------------------
# 1️⃣ Aktifkan mod_rewrite
RewriteEngine On

# Gantikan ikut root folder awak di URL
RewriteBase /ymhub/

# -----------------------------
# 2️⃣ Jika file/folder wujud, jangan rewrite
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# -----------------------------
# 3️⃣ Auto tambahkan .php jika wujud
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^(.*)/?$ $1.php [L,QSA]

# -----------------------------
# 4️⃣ Redirect .php ke clean URL
RewriteCond %{THE_REQUEST} \s([^.]+)\.php[?\s] [NC]
RewriteRule ^ %1 [R=301,L]

# -----------------------------
# 5️⃣ Optional: clear LSCache untuk dev
# Kalau masih 404, buat ini sekali je
# <IfModule LiteSpeed>
# CacheDisable public
# </IfModule>
